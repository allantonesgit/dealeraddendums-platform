{
  "name": "dealeraddendums-platform",
  "version": "1.0.0",
  "description": "Dealer Addendums internal tools platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop all",
    "restart": "pm2 restart all",
    "logs": "pm2 logs",
    "deploy": "bash scripts/deploy.sh"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
